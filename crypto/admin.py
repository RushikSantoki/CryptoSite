from django.contrib import admin
from CryptoSite import models

# Register your models here.
admin.site.register(models.Status)
admin.site.register(models.Icons)
admin.site.register(models.currencyUser)
